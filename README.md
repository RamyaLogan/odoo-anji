# Odoo 17 Self-Hosted with Docker

## Steps:

1. Clone Odoo source code into `odoo/` directory:

    git clone https://github.com/odoo/odoo.git -b 17.0 odoo

2. Build and start services:

    docker-compose up --build

3. Access Odoo:

    http://localhost:8069

Default Admin Password: `admin`
